import 'package:flutter/material.dart';

import '../constains/colors.dart';

BoxDecoration kHeadedDecoration = BoxDecoration(
    gradient:
        LinearGradient(colors: [Colors.transparent, CustomColors.bgLight11]),
    borderRadius: BorderRadius.circular(100));
